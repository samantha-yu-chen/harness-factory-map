{
  "fixture_version": "3.0",
  "fixture_id": "harness-factory-map.event-aligned",
  "source_repository": "harness-factory-map",
  "capability": "architecture-map.event-compatibility",
  "status": "legacy_behaviour",
  "given": {
    "specifications": [
      { "id": "producer-alpha", "events_emitted": ["record.changed"] },
      { "id": "consumer-beta", "events_consumed": ["record.changed"] }
    ]
  },
  "when": { "operation": "harness-factory-map.validation.event-drift" },
  "expected": {
    "outcome": "ALLOW",
    "code": "EVENT_PRODUCER_CONSUMER_ALIGNED",
    "result": {
      "admitted": true,
      "consumer": "consumer-beta",
      "event": "record.changed"
    },
    "owner": "none",
    "exit_condition": "none",
    "invalidated_evidence": []
  },
  "invariants": ["architecture-map.consumed-event-has-origin"],
  "legacy_context": {
    "adapter": {
      "id": "harness-factory-map.fixture-adapter.v3",
      "source_revision": "68d727c30a96cb8e046de2ce38fa9ee69af17e1e",
      "source_tests": ["harness-factory-map.test.fixture-adapter-registry"],
      "source_rules": ["harness-factory-map.rule.event-drift"],
      "verifier_rule": "harness-factory-map.verify.fixture-v3",
      "outcome_rule": "harness-factory-map.normalize.outcome-v3",
      "result_rule": "harness-factory-map.normalize.result-v3",
      "recovery_rule": "harness-factory-map.normalize.recovery-v3",
      "invalidated_evidence_rule": "none"
    },
    "normalization": "Adapter-derived shared outcome and recovery metadata: the executable recovery admits a consumed event once a matching producer exists."
  }
}
